// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.ChatMessage
{
    // 无
    // 刚提交
    // 处理中
    // 已回复
    // 已关闭
    // 游戏错误
    // 意见建议
    // 举报玩家
    /// 玩家提问 client->chat->track
    public sealed class PutQuestionMessage : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_QUESTION;
        public short GetMsgType { get { return MsgType; } }
        public uint happenTime;
        public int type;
        public string title;
        public string text;
        
        public static void Send(uint happenTime, int type, string title, string text, object className)
        {
            var packet = new PutQuestionMessage
            {
                happenTime = happenTime,
                type = type,
                title = title,
                text = text
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            happenTime = reader.ReadUInt32();
            type = reader.ReadInt32();
            title = reader.ReadString();
            text = reader.ReadString();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(happenTime);
            writer.Write(type);
            writer.Write(title);
            writer.Write(text);
        }
    }
}
