// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.ChatMessage
{
    /// 系统公告
    public sealed class SystemBulletinNotify : IProtocol
    {
        public const short MsgType = MessageType.MSG_SYSTEM_BULLETIN_NOTIFY;
        public short GetMsgType { get { return MsgType; } }
        public uint Pos;// 公告位置 enum BULLETIN_POSITION
        public string szColor;
        public string szContent;// 公告内容
        public string strPlatform;// 游戏平台标志。字段为空的时候，所有玩家都要显示
        public uint PlayItemId;// 播放特效物品id
        
        public static void Send(uint pos, string szColor, string szContent, string strPlatform, uint playItemId, object className)
        {
            var packet = new SystemBulletinNotify
            {
                Pos = pos,
                szColor = szColor,
                szContent = szContent,
                strPlatform = strPlatform,
                PlayItemId = playItemId
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            Pos = reader.ReadUInt32();
            szColor = reader.ReadString();
            szContent = reader.ReadString();
            strPlatform = reader.ReadString();
            PlayItemId = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(Pos);
            writer.Write(szColor);
            writer.Write(szContent);
            writer.Write(strPlatform);
            writer.Write(PlayItemId);
        }
    }
}
