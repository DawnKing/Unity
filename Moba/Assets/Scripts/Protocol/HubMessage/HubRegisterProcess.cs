// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.HubMessage
{
    // ----------------------------------------------------------------------
    ///	@file			protocol/HubMessage.h
    ///	@brief
    ///	@author			Bob Lee (libo@sixcube.cn)
    ///	@copyright		Sixcube Information Technology Co,. Ltd. (2012)
    ///	@date			2004-01-04 14:26:55
    // ----------------------------------------------------------------------
    /**
     * HUB请求：HUBSVR使用者根据进程ID注册
     * MSGTYPE_DECLARE(MSG_HUB_REGISTER_PROCESS)
     */
    public sealed class HubRegisterProcess : IProtocol
    {
        public const short MsgType = MessageType.MSG_HUB_SERVICE_REQUEST;
        public short GetMsgType { get { return MsgType; } }
        public uint procUid;// 全区唯一进程标识
        public string wanAddr;// 监听地址
        public int wanPort;// 监听的端口
        
        public static void Send(uint procUid, string wanAddr, int wanPort, object className)
        {
            var packet = new HubRegisterProcess
            {
                procUid = procUid,
                wanAddr = wanAddr,
                wanPort = wanPort
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            procUid = reader.ReadUInt32();
            wanAddr = reader.ReadString();
            wanPort = reader.ReadInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(procUid);
            writer.Write(wanAddr);
            writer.Write(wanPort);
        }
    }
}
