// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.QuestMessage
{
    /// 通知track全服充值任务数额
    public sealed class NotifyServerChargeNum : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_SERVER_CHARGE_NUM;
        public short GetMsgType { get { return MsgType; } }
        public uint[] vecChargeNum;
        
        public static void Send(uint[] vecChargeNum, object className)
        {
            var packet = new NotifyServerChargeNum
            {
                vecChargeNum = vecChargeNum
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            vecChargeNum = new uint[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                vecChargeNum[i1] = reader.ReadUInt32();
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(vecChargeNum == null ? 0 : vecChargeNum.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                writer.Write(vecChargeNum[i1]);
            }
        }
    }
}
