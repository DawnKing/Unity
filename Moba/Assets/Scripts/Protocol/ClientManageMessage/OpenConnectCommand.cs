// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.ClientManageMessage
{
    public sealed class OpenConnectCommand : IProtocol
    {
        public const short MsgType = MessageType.MSG_CLIENT_MANAGE_OPENCONNECT_COMMAND;
        public short GetMsgType { get { return MsgType; } }
        public string ip;
        public string port;
        
        public static void Send(string ip, string port, object className)
        {
            var packet = new OpenConnectCommand
            {
                ip = ip,
                port = port
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            ip = reader.ReadString();
            port = reader.ReadString();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(ip);
            writer.Write(port);
        }
    }
}
