// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TeamMessage
{
    // MSGTYPE_DECLARE(MSG_CHAR_ADD_TEAM_MERIT),
    // track->guild,增加玩家军团贡献
    public sealed class CharAddTeamMerit : IProtocol
    {
        public const short MsgType = MessageType.MSG_CHAR_ADD_TEAM_MERIT;
        public short GetMsgType { get { return MsgType; } }
        public CharMerit[] charVec;// 玩家列表
        
        public static void Send(CharMerit[] charVec, object className)
        {
            var packet = new CharAddTeamMerit
            {
                charVec = charVec
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            charVec = new CharMerit[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                charVec[i1] = new CharMerit();
                charVec[i1].ReadFromStream(reader);
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(charVec == null ? 0 : charVec.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                charVec[i1].WriteToStream(writer);
            }
        }
    }
}
