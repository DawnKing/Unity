// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

using GameProtocol.ShareTypes;
namespace GameProtocol.TeamMessage
{
    // MSGTYPE_DECLARE(MSG_NOTIFY_JOIN_GUILD),
    // 通知有玩家加入军团
    public sealed class NotifyJoinGuild : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_JOIN_GUILD;
        public short GetMsgType { get { return MsgType; } }
        public BUDDY_BRIEF newMember;// 通知新加入队员的信息
        public MemberInfo info;
        
        public static void Send(BUDDY_BRIEF newMember, MemberInfo info, object className)
        {
            var packet = new NotifyJoinGuild
            {
                newMember = newMember,
                info = info
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            newMember = new BUDDY_BRIEF();
            newMember.ReadFromStream(reader);
            info = new MemberInfo();
            info.ReadFromStream(reader);
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            newMember.WriteToStream(writer);
            info.WriteToStream(writer);
        }
    }
}
