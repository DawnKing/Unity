// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TeamMessage
{
    // MSGTYPE_DECLARE(MSG_NOTIFY_PLAYER_GET_GUILD_BOSS_AWARD),
    // 通知获得军团BOSS奖励
    public sealed class NotifyPlayerGetGuildBossAwardMessage : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_PLAYER_GET_GUILD_BOSS_AWARD;
        public short GetMsgType { get { return MsgType; } }
        public GuildBossAwardInfo awardInfo;// 奖励信息
        
        public static void Send(GuildBossAwardInfo awardInfo, object className)
        {
            var packet = new NotifyPlayerGetGuildBossAwardMessage
            {
                awardInfo = awardInfo
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            awardInfo = new GuildBossAwardInfo();
            awardInfo.ReadFromStream(reader);
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            awardInfo.WriteToStream(writer);
        }
    }
}
