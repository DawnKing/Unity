// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TeamMessage
{
    /// 请求团员移队
    public sealed class RequestMoveMember : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_GUILD_MOVE_MEMBER;
        public short GetMsgType { get { return MsgType; } }
        public uint OrigTeamId;// 源战队
        public string origUuid;// 源队员
        public uint DestTeamId;// 目标战队
        public string destUuid;// 目标队员
        
        public static void Send(uint origTeamId, string origUuid, uint destTeamId, string destUuid, object className)
        {
            var packet = new RequestMoveMember
            {
                OrigTeamId = origTeamId,
                origUuid = origUuid,
                DestTeamId = destTeamId,
                destUuid = destUuid
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            OrigTeamId = reader.ReadUInt32();
            origUuid = reader.ReadString();
            DestTeamId = reader.ReadUInt32();
            destUuid = reader.ReadString();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(OrigTeamId);
            writer.Write(origUuid);
            writer.Write(DestTeamId);
            writer.Write(destUuid);
        }
    }
}
