// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TeamMessage
{
    // MSGTYPE_DECLARE(MSG_CHAR_ADD_GUILD_ACTIVE),
    // gate->guild 玩家增加军团活跃度
    public sealed class CharAddGuildActive : IProtocol
    {
        public const short MsgType = MessageType.MSG_CHAR_ADD_GUILD_ACTIVE;
        public short GetMsgType { get { return MsgType; } }
        public uint CharId;// 角色ID
        public uint Active;// 活跃度
        public byte isLogin;// 是否是登录活跃
        
        public static void Send(uint charId, uint active, byte isLogin, object className)
        {
            var packet = new CharAddGuildActive
            {
                CharId = charId,
                Active = active,
                isLogin = isLogin
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            CharId = reader.ReadUInt32();
            Active = reader.ReadUInt32();
            isLogin = reader.ReadByte();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(CharId);
            writer.Write(Active);
            writer.Write(isLogin);
        }
    }
}
