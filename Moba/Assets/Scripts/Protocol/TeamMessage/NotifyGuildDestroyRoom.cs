// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TeamMessage
{
    // track->guild
    public sealed class NotifyGuildDestroyRoom : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_GUILD_DESTROY_ROOM;
        public short GetMsgType { get { return MsgType; } }
        public uint RoomType;// 房间类型 enum ROOM_TYPE
        public uint SvrUid;// track的sid
        public uint Id;// 根据房间类型的自定义ID
        
        public static void Send(uint roomType, uint svrUid, uint id, object className)
        {
            var packet = new NotifyGuildDestroyRoom
            {
                RoomType = roomType,
                SvrUid = svrUid,
                Id = id
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            RoomType = reader.ReadUInt32();
            SvrUid = reader.ReadUInt32();
            Id = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(RoomType);
            writer.Write(SvrUid);
            writer.Write(Id);
        }
    }
}
