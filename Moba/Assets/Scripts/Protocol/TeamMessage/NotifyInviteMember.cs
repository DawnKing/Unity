// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TeamMessage
{
    /// 通知邀请团员
    public sealed class NotifyInviteMember : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_GUILD_INVITE_MEMBER;
        public short GetMsgType { get { return MsgType; } }
        public string szStarter;// 发起者 名称
        public string charUuid;// 发起者 Uuid
        public uint TeamId;// 战队id
        public string TeamName;// 战队名
        
        public static void Send(string szStarter, string charUuid, uint teamId, string teamName, object className)
        {
            var packet = new NotifyInviteMember
            {
                szStarter = szStarter,
                charUuid = charUuid,
                TeamId = teamId,
                TeamName = teamName
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            szStarter = reader.ReadString();
            charUuid = reader.ReadString();
            TeamId = reader.ReadUInt32();
            TeamName = reader.ReadString();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(szStarter);
            writer.Write(charUuid);
            writer.Write(TeamId);
            writer.Write(TeamName);
        }
    }
}
