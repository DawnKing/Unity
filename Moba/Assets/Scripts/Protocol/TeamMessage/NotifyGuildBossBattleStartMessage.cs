// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TeamMessage
{
    // MSGTYPE_DECLARE(MSG_NOTIFY_GUILD_BOSS_BATTLE_START),
    // 通知军团BOSS战斗开始
    public sealed class NotifyGuildBossBattleStartMessage : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_GUILD_BOSS_BATTLE_START;
        public short GetMsgType { get { return MsgType; } }
        public string charUuid;// 玩家UUID
        public uint bossId;// BOSS模板ID
        public int bossDmgHp;// BOSS损失血量
        public uint mapId;// 地图ID
        
        public static void Send(string charUuid, uint bossId, int bossDmgHp, uint mapId, object className)
        {
            var packet = new NotifyGuildBossBattleStartMessage
            {
                charUuid = charUuid,
                bossId = bossId,
                bossDmgHp = bossDmgHp,
                mapId = mapId
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            charUuid = reader.ReadString();
            bossId = reader.ReadUInt32();
            bossDmgHp = reader.ReadInt32();
            mapId = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(charUuid);
            writer.Write(bossId);
            writer.Write(bossDmgHp);
            writer.Write(mapId);
        }
    }
}
