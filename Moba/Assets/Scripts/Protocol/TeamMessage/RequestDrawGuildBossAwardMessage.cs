// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TeamMessage
{
    // MSGTYPE_DECLARE(MSG_REQUEST_DRAW_GUILD_BOSS_AWARD),
    // 请求领取军团BOSS奖励
    public sealed class RequestDrawGuildBossAwardMessage : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_DRAW_GUILD_BOSS_AWARD;
        public short GetMsgType { get { return MsgType; } }
        public const int GBDT_BASE = 0;//基本奖励
        public const int GBDT_RAND = 1;//随机奖励
        public uint bossId;// BOSS模板ID
        public uint darwType;// 领取类型
        
        public static void Send(uint bossId, uint darwType, object className)
        {
            var packet = new RequestDrawGuildBossAwardMessage
            {
                bossId = bossId,
                darwType = darwType
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            bossId = reader.ReadUInt32();
            darwType = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(bossId);
            writer.Write(darwType);
        }
    }
}
