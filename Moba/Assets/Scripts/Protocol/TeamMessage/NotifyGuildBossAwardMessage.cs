// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TeamMessage
{
    // MSGTYPE_DECLARE(MSG_NOTIFY_GUILD_BOSS_AWARD),
    // 通知军团BOSS奖励信息
    public sealed class NotifyGuildBossAwardMessage : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_GUILD_BOSS_AWARD;
        public short GetMsgType { get { return MsgType; } }
        public GuildBossAwardInfo[] awardInfoVec;// 奖励信息
        
        public static void Send(GuildBossAwardInfo[] awardInfoVec, object className)
        {
            var packet = new NotifyGuildBossAwardMessage
            {
                awardInfoVec = awardInfoVec
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            awardInfoVec = new GuildBossAwardInfo[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                awardInfoVec[i1] = new GuildBossAwardInfo();
                awardInfoVec[i1].ReadFromStream(reader);
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(awardInfoVec == null ? 0 : awardInfoVec.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                awardInfoVec[i1].WriteToStream(writer);
            }
        }
    }
}
