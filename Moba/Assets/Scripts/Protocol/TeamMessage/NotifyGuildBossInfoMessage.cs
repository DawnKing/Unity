// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TeamMessage
{
    // MSGTYPE_DECLARE(MSG_NOTIFY_GUILD_BOSS_INFO),
    // 通知军团BOSS信息
    public sealed class NotifyGuildBossInfoMessage : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_GUILD_BOSS_INFO;
        public short GetMsgType { get { return MsgType; } }
        public GuildBossInfo[] bossInfoVec;// 奖励信息
        
        public static void Send(GuildBossInfo[] bossInfoVec, object className)
        {
            var packet = new NotifyGuildBossInfoMessage
            {
                bossInfoVec = bossInfoVec
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            bossInfoVec = new GuildBossInfo[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                bossInfoVec[i1] = new GuildBossInfo();
                bossInfoVec[i1].ReadFromStream(reader);
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(bossInfoVec == null ? 0 : bossInfoVec.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                bossInfoVec[i1].WriteToStream(writer);
            }
        }
    }
}
