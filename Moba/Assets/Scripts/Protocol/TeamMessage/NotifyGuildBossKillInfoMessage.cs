// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TeamMessage
{
    // MSGTYPE_DECLARE(MSG_NOTIFY_GUILD_BOSS_KILL_INFO),
    // 通知军团BOSS被击杀信息
    public sealed class NotifyGuildBossKillInfoMessage : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_GUILD_BOSS_KILL_INFO;
        public short GetMsgType { get { return MsgType; } }
        public KillGuildBossInfo[] infoVec;// 击杀信息
        
        public static void Send(KillGuildBossInfo[] infoVec, object className)
        {
            var packet = new NotifyGuildBossKillInfoMessage
            {
                infoVec = infoVec
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            infoVec = new KillGuildBossInfo[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                infoVec[i1] = new KillGuildBossInfo();
                infoVec[i1].ReadFromStream(reader);
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(infoVec == null ? 0 : infoVec.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                infoVec[i1].WriteToStream(writer);
            }
        }
    }
}
