// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TeamMessage
{
    // MSGTYPE_DECLARE(MSG_UPDATE_TEAM_INFO),
    // 更新战队信息
    public sealed class UpdateTeamInfo : IProtocol
    {
        public const short MsgType = MessageType.MSG_UPDATE_TEAM_INFO;
        public short GetMsgType { get { return MsgType; } }
        public TeamInfo info;// 战队信息
        public uint Type;// UPDATE_TEAM_TYPE
        
        public static void Send(TeamInfo info, uint type, object className)
        {
            var packet = new UpdateTeamInfo
            {
                info = info,
                Type = type
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            info = new TeamInfo();
            info.ReadFromStream(reader);
            Type = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            info.WriteToStream(writer);
            writer.Write(Type);
        }
    }
}
