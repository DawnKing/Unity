// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TeamMessage
{
    /// 通知军团技能信息
    public sealed class NotifyGuildSkillInfo : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_GUILD_SKILL_INFO;
        public short GetMsgType { get { return MsgType; } }
        public GuildSkillInfo[] skillList;// 技能信息
        public uint OpType;// 操作类型 INFO_OP_TYPE
        
        public static void Send(GuildSkillInfo[] skillList, uint opType, object className)
        {
            var packet = new NotifyGuildSkillInfo
            {
                skillList = skillList,
                OpType = opType
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            skillList = new GuildSkillInfo[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                skillList[i1] = new GuildSkillInfo();
                skillList[i1].ReadFromStream(reader);
            }
            OpType = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(skillList == null ? 0 : skillList.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                skillList[i1].WriteToStream(writer);
            }
            writer.Write(OpType);
        }
    }
}
