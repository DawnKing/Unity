// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TeamMessage
{
    // MSGTYPE_DECLARE(MSG_REQUEST_JOIN_GUILD_APPLY),
    // 请求发送加入军团申请
    public sealed class RequestJoinApply : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_JOIN_GUILD_APPLY;
        public short GetMsgType { get { return MsgType; } }
        public uint GuildId;// 军团id
        public string szInviteName;// 邀请者名字
        public byte isFastJoin;// 是否是快速加入
        
        public static void Send(uint guildId, string szInviteName, byte isFastJoin, object className)
        {
            var packet = new RequestJoinApply
            {
                GuildId = guildId,
                szInviteName = szInviteName,
                isFastJoin = isFastJoin
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            GuildId = reader.ReadUInt32();
            szInviteName = reader.ReadString();
            isFastJoin = reader.ReadByte();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(GuildId);
            writer.Write(szInviteName);
            writer.Write(isFastJoin);
        }
    }
}
