// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.EchoMessage
{
    /// MSGTYPE_DECLARE(MSG_ECHO_RESPONSE)
    public sealed class EchoResponse : IProtocol
    {
        public const short MsgType = MessageType.MSG_ECHO_RESPONSE;
        public short GetMsgType { get { return MsgType; } }
        public uint ObjId;// 对象id
        public string szText;
        
        public static void Send(uint objId, string szText, object className)
        {
            var packet = new EchoResponse
            {
                ObjId = objId,
                szText = szText
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            ObjId = reader.ReadUInt32();
            szText = reader.ReadString();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(ObjId);
            writer.Write(szText);
        }
    }
}
