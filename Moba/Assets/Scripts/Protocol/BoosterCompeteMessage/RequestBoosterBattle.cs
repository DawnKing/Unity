// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.BoosterCompeteMessage
{
    /**
     * 请求进行助手对战
     * MSGTYPE_DECLARE(MSG_REQUEST_BOOSTER_BATTLE)
     */
    public sealed class RequestBoosterBattle : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_BOOSTER_BATTLE;
        public short GetMsgType { get { return MsgType; } }
        public uint BattleRank;/// 被挑战者的名次
        public string strEnemyUuid;/// 被挑战者的Uuid
        public uint SelfRank;/// 玩家自身排名，用于验证数据的时效性
        
        public static void Send(uint battleRank, string strEnemyUuid, uint selfRank, object className)
        {
            var packet = new RequestBoosterBattle
            {
                BattleRank = battleRank,
                strEnemyUuid = strEnemyUuid,
                SelfRank = selfRank
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            BattleRank = reader.ReadUInt32();
            strEnemyUuid = reader.ReadString();
            SelfRank = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(BattleRank);
            writer.Write(strEnemyUuid);
            writer.Write(SelfRank);
        }
    }
}
