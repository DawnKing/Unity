// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.BoosterCompeteMessage
{
    /**
     * 通知助手竞技场对手信息
     * MSGTYPE_DECLARE(MSG_NOTIFY_BOOSTER_ENEMY_LIST)
     */
    public sealed class NotifyBoosterEnemyList : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_BOOSTER_ENEMY_LIST;
        public short GetMsgType { get { return MsgType; } }
        public BOOSTERCOMP_BRIEF[] vecEnemy;/// 对手列表
        
        public static void Send(BOOSTERCOMP_BRIEF[] vecEnemy, object className)
        {
            var packet = new NotifyBoosterEnemyList
            {
                vecEnemy = vecEnemy
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            vecEnemy = new BOOSTERCOMP_BRIEF[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                vecEnemy[i1] = new BOOSTERCOMP_BRIEF();
                vecEnemy[i1].ReadFromStream(reader);
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(vecEnemy == null ? 0 : vecEnemy.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                vecEnemy[i1].WriteToStream(writer);
            }
        }
    }
}
