// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TankPartMessage
{
    // ----------------------------------------------------------------------
    // 坦克部件ID
    // 引擎
    // 主炮
    // 装甲
    // 副炮
    // 连锁
    // 坦克部件品质
    // 白
    // 绿
    // 蓝
    // 紫
    // 橙
    // 坦克部件操作类型
    // 充能
    // 升级
    // 消耗
    // 降级
    // 登录通知
    // 请求操作坦克部件
    public sealed class RequestOperateTankPart : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_OPERATE_TANKPART;
        public short GetMsgType { get { return MsgType; } }
        public uint nPartId;// 部件ID enum TANK_PART_ID
        public byte bOpeType;// 操作类型 enum TANK_PART_OPE_TYPE
        
        public static void Send(uint nPartId, byte bOpeType, object className)
        {
            var packet = new RequestOperateTankPart
            {
                nPartId = nPartId,
                bOpeType = bOpeType
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            nPartId = reader.ReadUInt32();
            bOpeType = reader.ReadByte();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(nPartId);
            writer.Write(bOpeType);
        }
    }
}
