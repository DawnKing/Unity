// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

using GameProtocol.BulkDataType;
namespace GameProtocol.TankPartMessage
{
    // 通知坦克部件信息
    public sealed class NotifyTankPartInfo : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_TANKPART_INFO;
        public short GetMsgType { get { return MsgType; } }
        public TankPartInfo[] partInfo;// 所有部件信息
        public byte bOpeType;// 操作类型 enum TANK_PART_OPE_TYPE
        
        public static void Send(TankPartInfo[] partInfo, byte bOpeType, object className)
        {
            var packet = new NotifyTankPartInfo
            {
                partInfo = partInfo,
                bOpeType = bOpeType
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            partInfo = new TankPartInfo[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                partInfo[i1] = new TankPartInfo();
                partInfo[i1].ReadFromStream(reader);
            }
            bOpeType = reader.ReadByte();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(partInfo == null ? 0 : partInfo.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                partInfo[i1].WriteToStream(writer);
            }
            writer.Write(bOpeType);
        }
    }
}
