// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.SceneMessage
{
    // MSGTYPE_DECLARE(MSG_GAME_SCENE_RECORD_START),
    // track->game 请求开始战斗录像
    public sealed class GameSceneRecordStart : IProtocol
    {
        public const short MsgType = MessageType.MSG_GAME_SCENE_RECORD_START;
        public short GetMsgType { get { return MsgType; } }
        public uint SceneId;//战场ID
        public string urlParam;// 上传录像参数
        public string strRecord;// 录像数据
        
        public static void Send(uint sceneId, string urlParam, string strRecord, object className)
        {
            var packet = new GameSceneRecordStart
            {
                SceneId = sceneId,
                urlParam = urlParam,
                strRecord = strRecord
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            SceneId = reader.ReadUInt32();
            urlParam = reader.ReadString();
            strRecord = reader.ReadString();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(SceneId);
            writer.Write(urlParam);
            writer.Write(strRecord);
        }
    }
}
