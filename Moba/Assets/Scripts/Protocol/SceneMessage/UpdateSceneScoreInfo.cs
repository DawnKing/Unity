// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

using GameProtocol.BulkDataType;
namespace GameProtocol.SceneMessage
{
    /// 更新整个战场的分数信息
    public sealed class UpdateSceneScoreInfo : IProtocol
    {
        public const short MsgType = MessageType.MSG_UPDATE_SCENE_ONE_STAT;
        public short GetMsgType { get { return MsgType; } }
        public OneObjScore[] values;// 战场内所有的得分值
        
        public static void Send(OneObjScore[] values, object className)
        {
            var packet = new UpdateSceneScoreInfo
            {
                values = values
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            values = new OneObjScore[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                values[i1] = new OneObjScore();
                values[i1].ReadFromStream(reader);
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(values == null ? 0 : values.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                values[i1].WriteToStream(writer);
            }
        }
    }
}
