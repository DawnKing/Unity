// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.SceneMessage
{
    // MSGTYPE_DECLARE(MSG_NOTIFY_GUILD_BOSS_END_DAMAGE_INFO),
    // 通知军团Boss战斗结束信息
    public sealed class NotifyGuildBossEndDamageInfoMessage : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_GUILD_BOSS_END_DAMAGE_INFO;
        public short GetMsgType { get { return MsgType; } }
        public uint sceneId;// 场景ID
        public int guildBossDamageHp;// 军团BOSS被伤害血量
        public uint isDead;// 是否已死亡
        
        public static void Send(uint sceneId, int guildBossDamageHp, uint isDead, object className)
        {
            var packet = new NotifyGuildBossEndDamageInfoMessage
            {
                sceneId = sceneId,
                guildBossDamageHp = guildBossDamageHp,
                isDead = isDead
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            sceneId = reader.ReadUInt32();
            guildBossDamageHp = reader.ReadInt32();
            isDead = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(sceneId);
            writer.Write(guildBossDamageHp);
            writer.Write(isDead);
        }
    }
}
