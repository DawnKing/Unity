// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.SceneMessage
{
    // MSGTYPE_DECLARE(MSG_GAME_CHANGE_BATTLE_TECHNO),
    // 通知更新技术统计
    public sealed class ChangeBattleTechno : IProtocol
    {
        public const short MsgType = MessageType.MSG_GAME_CHANGE_BATTLE_TECHNO;
        public short GetMsgType { get { return MsgType; } }
        public short Res;// 战斗结果,0,失败;1,获胜;-1,逃跑
        public byte isKiller;// 是否是杀戮者
        public byte isAstKing;// 是否是助攻王
        public byte isKDA;// 是否是全能王
        public byte isDestroyer;// 是否是拆迁王
        public byte roomType;// 房间类型 room_type
        public byte Kill;// 本场击杀数
        public byte Ast;// 本场助攻数
        public short UpEloVal;// 战力变化值
        public short EnemyElo;// 敌方平均战力
        public ushort eloSectionId;// 天梯ELO阶段ID
        
        public static void Send(short res, byte isKiller, byte isAstKing, byte isKDA, byte isDestroyer, byte roomType, byte kill, byte ast, short upEloVal, short enemyElo, ushort eloSectionId, object className)
        {
            var packet = new ChangeBattleTechno
            {
                Res = res,
                isKiller = isKiller,
                isAstKing = isAstKing,
                isKDA = isKDA,
                isDestroyer = isDestroyer,
                roomType = roomType,
                Kill = kill,
                Ast = ast,
                UpEloVal = upEloVal,
                EnemyElo = enemyElo,
                eloSectionId = eloSectionId
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            Res = reader.ReadInt16();
            isKiller = reader.ReadByte();
            isAstKing = reader.ReadByte();
            isKDA = reader.ReadByte();
            isDestroyer = reader.ReadByte();
            roomType = reader.ReadByte();
            Kill = reader.ReadByte();
            Ast = reader.ReadByte();
            UpEloVal = reader.ReadInt16();
            EnemyElo = reader.ReadInt16();
            eloSectionId = reader.ReadUInt16();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(Res);
            writer.Write(isKiller);
            writer.Write(isAstKing);
            writer.Write(isKDA);
            writer.Write(isDestroyer);
            writer.Write(roomType);
            writer.Write(Kill);
            writer.Write(Ast);
            writer.Write(UpEloVal);
            writer.Write(EnemyElo);
            writer.Write(eloSectionId);
        }
    }
}
