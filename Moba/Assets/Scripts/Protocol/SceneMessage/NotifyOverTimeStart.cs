// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.SceneMessage
{
    // MSGTYPE_DECLARE(MSG_NOTIFY_OVERTIME_START),
    // 通知加时赛开始, 删除所有坦克对象
    public sealed class NotifyOverTimeStart : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_OVERTIME_START;
        public short GetMsgType { get { return MsgType; } }
        public uint overTime;// 加时赛的比赛时间
        
        public static void Send(uint overTime, object className)
        {
            var packet = new NotifyOverTimeStart
            {
                overTime = overTime
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            overTime = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(overTime);
        }
    }
}
