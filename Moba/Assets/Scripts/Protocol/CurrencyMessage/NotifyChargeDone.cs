// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.CurrencyMessage
{
    /// 通知充值到帐
    public sealed class NotifyChargeDone : IProtocol
    {
        public const short MsgType = MessageType.MSG_CHARGE_DONE;
        public short GetMsgType { get { return MsgType; } }
        public int rmbCount;// rmb数量
        public int cashCount;// 充值到帐点券
        public uint giftPack;// 获得的礼包
        public int giftCount;// 赠送的礼券
        public int superVipBack;// 超级VIP返还
        
        public static void Send(int rmbCount, int cashCount, uint giftPack, int giftCount, int superVipBack, object className)
        {
            var packet = new NotifyChargeDone
            {
                rmbCount = rmbCount,
                cashCount = cashCount,
                giftPack = giftPack,
                giftCount = giftCount,
                superVipBack = superVipBack
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            rmbCount = reader.ReadInt32();
            cashCount = reader.ReadInt32();
            giftPack = reader.ReadUInt32();
            giftCount = reader.ReadInt32();
            superVipBack = reader.ReadInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(rmbCount);
            writer.Write(cashCount);
            writer.Write(giftPack);
            writer.Write(giftCount);
            writer.Write(superVipBack);
        }
    }
}
