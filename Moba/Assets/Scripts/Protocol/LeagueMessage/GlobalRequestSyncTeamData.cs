// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.LeagueMessage
{
    /// 请求同步战队成员信息 global->track
    public sealed class GlobalRequestSyncTeamData : IProtocol
    {
        public const short MsgType = MessageType.MSG_GLOBAL_REQUEST_SYNC_TEAM_MEMBER;
        public short GetMsgType { get { return MsgType; } }
        public uint TeamId;// 需要同步的战队ID
        public uint ContestId;// 对阵ID
        
        public static void Send(uint teamId, uint contestId, object className)
        {
            var packet = new GlobalRequestSyncTeamData
            {
                TeamId = teamId,
                ContestId = contestId
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            TeamId = reader.ReadUInt32();
            ContestId = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(TeamId);
            writer.Write(ContestId);
        }
    }
}
