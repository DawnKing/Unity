// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.LeagueMessage
{
    /// 通知联赛自动报名信息
    public sealed class NotifyLeagueAutoEnroll : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_LEAGUE_AUTO_ENROLL;
        public short GetMsgType { get { return MsgType; } }
        public uint session;// 赛事实例
        public uint zone_id;// 分区
        public uint teaplace;// 自动报名的战队名次
        
        public static void Send(uint session, uint zone_id, uint teaplace, object className)
        {
            var packet = new NotifyLeagueAutoEnroll
            {
                session = session,
                zone_id = zone_id,
                teaplace = teaplace
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            session = reader.ReadUInt32();
            zone_id = reader.ReadUInt32();
            teaplace = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(session);
            writer.Write(zone_id);
            writer.Write(teaplace);
        }
    }
}
