// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.AwardMessage
{
    // MSGTYPE_DECLARE(MSG_NOTIFY_SEND_REDPACKET_SUCC),
    // 通知发红包成功
    public sealed class NotifySendRedPacketSucc : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_SEND_REDPACKET_SUCC;
        public short GetMsgType { get { return MsgType; } }
        public RedPktLog pktLog;// 红包记录
        
        public static void Send(RedPktLog pktLog, object className)
        {
            var packet = new NotifySendRedPacketSucc
            {
                pktLog = pktLog
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            pktLog = new RedPktLog();
            pktLog.ReadFromStream(reader);
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            pktLog.WriteToStream(writer);
        }
    }
}
