// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.VipMessage
{
    // MSGTYPE_DECLARE(MSG_NOTIFY_QQ_VIP),
    // 通知qq黄钻蓝钻信息
    public sealed class NotifyQQVip : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_QQ_VIP;
        public short GetMsgType { get { return MsgType; } }
        public uint YellowLevel;// 黄钻等级
        public byte IsYearYellow;// 是否是年费黄钻用户,0:不是;1:是
        public byte IsHighYellow;// 是否豪华版黄钻,0:不是;1:是
        public uint BlueLevel;// 蓝钻等级
        public byte IsYearBlue;// 是否是年费蓝钻用户,0:不是;1:是
        public byte IsHighBlue;// 是否豪华版蓝钻,0:不是;1:是
        public uint m_n3366Level;// 3366等级
        
        public static void Send(uint yellowLevel, byte isYearYellow, byte isHighYellow, uint blueLevel, byte isYearBlue, byte isHighBlue, uint m_n3366Level, object className)
        {
            var packet = new NotifyQQVip
            {
                YellowLevel = yellowLevel,
                IsYearYellow = isYearYellow,
                IsHighYellow = isHighYellow,
                BlueLevel = blueLevel,
                IsYearBlue = isYearBlue,
                IsHighBlue = isHighBlue,
                m_n3366Level = m_n3366Level
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            YellowLevel = reader.ReadUInt32();
            IsYearYellow = reader.ReadByte();
            IsHighYellow = reader.ReadByte();
            BlueLevel = reader.ReadUInt32();
            IsYearBlue = reader.ReadByte();
            IsHighBlue = reader.ReadByte();
            m_n3366Level = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(YellowLevel);
            writer.Write(IsYearYellow);
            writer.Write(IsHighYellow);
            writer.Write(BlueLevel);
            writer.Write(IsYearBlue);
            writer.Write(IsHighBlue);
            writer.Write(m_n3366Level);
        }
    }
}
