// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.ChapterMessage
{
    // MSGTYPE_DECLARE(MSG_PVE_REQUEST_TANK_TRIAL_MAP)
    // 请求选择坦克开始试玩副本
    public sealed class RequestSelectTankTrialMap : IProtocol
    {
        public const short MsgType = MessageType.MSG_PVE_REQUEST_TANK_TRIAL_MAP;
        public short GetMsgType { get { return MsgType; } }
        public uint MapId;// 关卡编号
        public uint TankId;// 坦克模板ID
        
        public static void Send(uint mapId, uint tankId, object className)
        {
            var packet = new RequestSelectTankTrialMap
            {
                MapId = mapId,
                TankId = tankId
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            MapId = reader.ReadUInt32();
            TankId = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(MapId);
            writer.Write(TankId);
        }
    }
}
