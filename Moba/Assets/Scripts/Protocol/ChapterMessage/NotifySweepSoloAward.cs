// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

using GameProtocol.BulkDataType;
namespace GameProtocol.ChapterMessage
{
    // MSGTYPE_DECLARE(MSG_NOTIFY_SOLO_SWEEP_AWARD),
    // 通知客户端，扫荡结果
    public sealed class NotifySweepSoloAward : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_SOLO_SWEEP_AWARD;
        public short GetMsgType { get { return MsgType; } }
        public SettleAwardInfo[] awards;// 扫荡奖励
        
        public static void Send(SettleAwardInfo[] awards, object className)
        {
            var packet = new NotifySweepSoloAward
            {
                awards = awards
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            awards = new SettleAwardInfo[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                awards[i1] = new SettleAwardInfo();
                awards[i1].ReadFromStream(reader);
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(awards == null ? 0 : awards.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                awards[i1].WriteToStream(writer);
            }
        }
    }
}
