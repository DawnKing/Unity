// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.ChapterMessage
{
    // MSGTYPE_DECLARE(MSG_REJECT_SELECT_SCENE),
    // 通知客户端，拒绝玩家打开某个关卡
    public sealed class RejectSelectScene : IProtocol
    {
        public const short MsgType = MessageType.MSG_REJECT_SELECT_SCENE;
        public short GetMsgType { get { return MsgType; } }
        public byte Reason;// 拒绝理由
        public uint Info;// 进一步的信息（如需要的每日可通关次数等）
        
        public static void Send(byte reason, uint info, object className)
        {
            var packet = new RejectSelectScene
            {
                Reason = reason,
                Info = info
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            Reason = reader.ReadByte();
            Info = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(Reason);
            writer.Write(Info);
        }
    }
}
