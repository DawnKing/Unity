// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TankMessage
{
    // 通知喷图成功
    public sealed class NotifySpray : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_OP_SPRAY;
        public short GetMsgType { get { return MsgType; } }
        public uint sprayId;// 图案的模板id
        public uint tankId;// 喷图者的id
        public short x;// x 坐标
        public short y;// y 坐标
        
        public static void Send(uint sprayId, uint tankId, short x, short y, object className)
        {
            var packet = new NotifySpray
            {
                sprayId = sprayId,
                tankId = tankId,
                x = x,
                y = y
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            sprayId = reader.ReadUInt32();
            tankId = reader.ReadUInt32();
            x = reader.ReadInt16();
            y = reader.ReadInt16();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(sprayId);
            writer.Write(tankId);
            writer.Write(x);
            writer.Write(y);
        }
    }
}
