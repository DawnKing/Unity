// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

using GameProtocol.BulkDataType;
namespace GameProtocol.TankMessage
{
    // MSGTYPE_DECLARE(MSG_SYNC_SKILL_COOLTIME),
    // 同步技能冷却时间
    public sealed class SyncSkillCooltime : IProtocol
    {
        public const short MsgType = MessageType.MSG_SYNC_SKILL_COOLTIME;
        public short GetMsgType { get { return MsgType; } }
        public PropsInfo PropData;// 技能信息
        public ulong OwnerId;// 拥有者ID
        
        public static void Send(PropsInfo propData, ulong ownerId, object className)
        {
            var packet = new SyncSkillCooltime
            {
                PropData = propData,
                OwnerId = ownerId
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            PropData = new PropsInfo();
            PropData.ReadFromStream(reader);
            OwnerId = reader.ReadUInt64();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            PropData.WriteToStream(writer);
            writer.Write(OwnerId);
        }
    }
}
