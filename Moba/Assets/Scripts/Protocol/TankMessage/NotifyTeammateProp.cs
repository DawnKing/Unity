// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TankMessage
{
    /*
     * server->client 上线通知队友大招信息
     */
    public sealed class NotifyTeammateProp : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_TEAMMATE_CD;
        public short GetMsgType { get { return MsgType; } }
        public TeamProp[] teamMateProp;
        
        public static void Send(TeamProp[] teamMateProp, object className)
        {
            var packet = new NotifyTeammateProp
            {
                teamMateProp = teamMateProp
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            teamMateProp = new TeamProp[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                teamMateProp[i1] = new TeamProp();
                teamMateProp[i1].ReadFromStream(reader);
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(teamMateProp == null ? 0 : teamMateProp.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                teamMateProp[i1].WriteToStream(writer);
            }
        }
    }
}
