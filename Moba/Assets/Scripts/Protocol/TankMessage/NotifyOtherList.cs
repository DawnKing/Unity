// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TankMessage
{
    /*
     * server->client 上线通知其他玩家数据
     */
    public sealed class NotifyOtherList : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_OTHER_LIST;
        public short GetMsgType { get { return MsgType; } }
        public TankInfo[] otherList;// 其他坦克
        
        public static void Send(TankInfo[] otherList, object className)
        {
            var packet = new NotifyOtherList
            {
                otherList = otherList
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            otherList = new TankInfo[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                otherList[i1] = new TankInfo();
                otherList[i1].ReadFromStream(reader);
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(otherList == null ? 0 : otherList.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                otherList[i1].WriteToStream(writer);
            }
        }
    }
}
