// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TankMessage
{
    // MSGTYPE_DECLARE(MSG_NOTIFY_SKILL_STOP),
    // 通知客户端技能被停止
    public sealed class NotifySkillStop : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_SKILL_STOP;
        public short GetMsgType { get { return MsgType; } }
        public ulong CasterId;// 释放者ID
        public uint SkillId;// 使用的技能
        
        public static void Send(ulong casterId, uint skillId, object className)
        {
            var packet = new NotifySkillStop
            {
                CasterId = casterId,
                SkillId = skillId
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            CasterId = reader.ReadUInt64();
            SkillId = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(CasterId);
            writer.Write(SkillId);
        }
    }
}
