// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.ActivityMessage
{
    // MSGTYPE_DECLARE(MSG_NOTIFY_NEXT_ONLINE_PEAK_ACT_START),
    // 通知开始下个阶段在线峰值活动
    public sealed class NotifyNextOnlinePeakActStartMessage : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_NEXT_ONLINE_PEAK_ACT_START;
        public short GetMsgType { get { return MsgType; } }
        public int onlinePeakPhase;//峰值在线活动阶段
        public int isNextStart;//是否开启下一阶段
        
        public static void Send(int onlinePeakPhase, int isNextStart, object className)
        {
            var packet = new NotifyNextOnlinePeakActStartMessage
            {
                onlinePeakPhase = onlinePeakPhase,
                isNextStart = isNextStart
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            onlinePeakPhase = reader.ReadInt32();
            isNextStart = reader.ReadInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(onlinePeakPhase);
            writer.Write(isNextStart);
        }
    }
}
