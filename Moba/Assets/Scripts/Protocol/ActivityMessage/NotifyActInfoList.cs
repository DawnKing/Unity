// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.ActivityMessage
{
    // 通知多个活动信息
    public sealed class NotifyActInfoList : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_ACT_INFO_LIST;
        public short GetMsgType { get { return MsgType; } }
        public ActInfo[] actList;// 活动列表
        
        public static void Send(ActInfo[] actList, object className)
        {
            var packet = new NotifyActInfoList
            {
                actList = actList
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            actList = new ActInfo[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                actList[i1] = new ActInfo();
                actList[i1].ReadFromStream(reader);
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(actList == null ? 0 : actList.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                actList[i1].WriteToStream(writer);
            }
        }
    }
}
