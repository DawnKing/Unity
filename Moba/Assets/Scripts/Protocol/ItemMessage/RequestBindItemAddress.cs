// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.ItemMessage
{
    /// 请求绑定物品地址
    public sealed class RequestBindItemAddress : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_BIND_ITEM_ADDRESS;
        public short GetMsgType { get { return MsgType; } }
        public string strItemUuid;/// 物品UUID
        public string RealName;/// 玩家真实名字
        public string RealAddress;/// 玩家真实地址
        public string PhoneNumber;/// 玩家联系电话
        public string QQ;/// 玩家的QQ号
        
        public static void Send(string strItemUuid, string realName, string realAddress, string phoneNumber, string qQ, object className)
        {
            var packet = new RequestBindItemAddress
            {
                strItemUuid = strItemUuid,
                RealName = realName,
                RealAddress = realAddress,
                PhoneNumber = phoneNumber,
                QQ = qQ
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            strItemUuid = reader.ReadString();
            RealName = reader.ReadString();
            RealAddress = reader.ReadString();
            PhoneNumber = reader.ReadString();
            QQ = reader.ReadString();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(strItemUuid);
            writer.Write(RealName);
            writer.Write(RealAddress);
            writer.Write(PhoneNumber);
            writer.Write(QQ);
        }
    }
}
