// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

using GameProtocol.BulkDataType;
namespace GameProtocol.ItemMessage
{
    /// 通知svr_award的近期获奖列表
    public sealed class NotifySvrAwardList : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_SVR_AWARD_LIST;
        public short GetMsgType { get { return MsgType; } }
        public uint Type;// SVR_AWARD_TYPE
        public OneSvrAward[] list;
        
        public static void Send(uint type, OneSvrAward[] list, object className)
        {
            var packet = new NotifySvrAwardList
            {
                Type = type,
                list = list
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            Type = reader.ReadUInt32();
            var length2 = reader.ReadUInt16();
            list = new OneSvrAward[length2];
            for (var i2 = 0; i2 < length2; i2++)
            {
                list[i2] = new OneSvrAward();
                list[i2].ReadFromStream(reader);
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(Type);
            ushort count2 = (ushort)(list == null ? 0 : list.Length);
            writer.Write(count2);
            for(var i2 = 0; i2 < count2; i2++)
            {
                list[i2].WriteToStream(writer);
            }
        }
    }
}
