// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

using GameProtocol.BulkDataType;
namespace GameProtocol.ItemMessage
{
    public sealed class NotifySettleAward : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_SETTLE_AWARD;
        public short GetMsgType { get { return MsgType; } }
        public uint opType;// 物品操作类型 enum SETTLE_TYPE
        public SettleAwardInfo info;
        public byte isShow;// 是否展示
        public uint ExtInfo;// 附加信息
        
        public static void Send(uint opType, SettleAwardInfo info, byte isShow, uint extInfo, object className)
        {
            var packet = new NotifySettleAward
            {
                opType = opType,
                info = info,
                isShow = isShow,
                ExtInfo = extInfo
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            opType = reader.ReadUInt32();
            info = new SettleAwardInfo();
            info.ReadFromStream(reader);
            isShow = reader.ReadByte();
            ExtInfo = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(opType);
            info.WriteToStream(writer);
            writer.Write(isShow);
            writer.Write(ExtInfo);
        }
    }
}
