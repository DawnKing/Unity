// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.NpcMessage
{
    // MSGTYPE_DECLARE(MSG_NPC_REPAIR_TANK),
    // npc->gamesvr 通知修理箱给坦克补血
    public sealed class NpcRepairTank : IProtocol
    {
        public const short MsgType = MessageType.MSG_NPC_REPAIR_TANK;
        public short GetMsgType { get { return MsgType; } }
        public uint SceneId;// 场景id
        public ulong NpcId;// 修理箱id
        public ulong TankId;// 坦克id
        
        public static void Send(uint sceneId, ulong npcId, ulong tankId, object className)
        {
            var packet = new NpcRepairTank
            {
                SceneId = sceneId,
                NpcId = npcId,
                TankId = tankId
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            SceneId = reader.ReadUInt32();
            NpcId = reader.ReadUInt64();
            TankId = reader.ReadUInt64();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(SceneId);
            writer.Write(NpcId);
            writer.Write(TankId);
        }
    }
}
