// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.MonitorMessage
{
    /// MSGTYPE_DECLARE(MSG_NOTIFY_NET_PROFILE_DATA),
    /// 返回服务器返回网络性能分析数据
    public sealed class NotifyNetProfileDataMessage : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_NET_PROFILE_DATA;
        public short GetMsgType { get { return MsgType; } }
        public uint serverUid;// 服务器进程标识
        public string serverName;// 服务器名
        public NetProfileData netProfileData;// 服务器性能分析数据
        
        public static void Send(uint serverUid, string serverName, NetProfileData netProfileData, object className)
        {
            var packet = new NotifyNetProfileDataMessage
            {
                serverUid = serverUid,
                serverName = serverName,
                netProfileData = netProfileData
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            serverUid = reader.ReadUInt32();
            serverName = reader.ReadString();
            netProfileData = new NetProfileData();
            netProfileData.ReadFromStream(reader);
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(serverUid);
            writer.Write(serverName);
            netProfileData.WriteToStream(writer);
        }
    }
}
