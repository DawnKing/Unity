// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.MonitorMessage
{
    /// 被管理对象的类型
    // 大区
    // 单节点集群
    // 物理节点
    // 进程组(单服多组的情况)
    // 进程
    /// 进程组结构定义
    public sealed class ProcGroup
    {
        public string szName;// 进程组的名字
        public string[] aProcs;// 组内进程列表
        public void ReadFromStream(BinaryReader reader)
        {
            szName = reader.ReadString();
            var length2 = reader.ReadUInt16();
            aProcs = new string[length2];
            for (var i2 = 0; i2 < length2; i2++)
            {
                aProcs[i2] = reader.ReadString();
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(szName);
            ushort count2 = (ushort)(aProcs == null ? 0 : aProcs.Length);
            writer.Write(count2);
            for(var i2 = 0; i2 < count2; i2++)
            {
                writer.Write(aProcs[i2]);
            }
        }
    }
}
