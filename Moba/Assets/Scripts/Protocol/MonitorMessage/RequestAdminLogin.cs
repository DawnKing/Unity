// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.MonitorMessage
{
    /**
    * 请求登陆管理服
    * MSG_ADMIN_REQUEST_LOGIN
    */
    public sealed class RequestAdminLogin : IProtocol
    {
        public const short MsgType = MessageType.MSG_ADMIN_REQUEST_LOGIN;
        public short GetMsgType { get { return MsgType; } }
        public uint version;
        public string login;// 登录名
        public string password;// 登录密码
        
        public static void Send(uint version, string login, string password, object className)
        {
            var packet = new RequestAdminLogin
            {
                version = version,
                login = login,
                password = password
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            version = reader.ReadUInt32();
            login = reader.ReadString();
            password = reader.ReadString();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(version);
            writer.Write(login);
            writer.Write(password);
        }
    }
}
