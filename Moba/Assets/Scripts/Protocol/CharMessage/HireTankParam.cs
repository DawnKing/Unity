// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.CharMessage
{
    // 租用坦克参数信息
    public sealed class HireTankParam
    {
        public const int RESET_NONE = 0;// 没有重置周期
        public const int RESET_DAY = 1;// 每日重置(0点)
        public const int RESET_WEEK = 2;// 每周重置(周一0点)
        public const int RESET_MONTH = 3;// 每月重置
        public uint Id;// ID
        public int Level;// 等级
        public uint coinType;// coin_type 货币类型
        public int countMax;// 租用次数上限 0：无限 通常用于银币租用
        public int Hour;// 租用时间 小时计算
        public int Fee;// 租用坦克的初始费用  费用=fee * feerate ^ 次数
        public float fFeeRate;// 租用坦克的费用增长率
        public int FeeMax;// 坦克租用的费用上限 为0表示免费
        public uint ResetType;// 重置类型（刷新租用次数的周期）
        public void ReadFromStream(BinaryReader reader)
        {
            Id = reader.ReadUInt32();
            Level = reader.ReadInt32();
            coinType = reader.ReadUInt32();
            countMax = reader.ReadInt32();
            Hour = reader.ReadInt32();
            Fee = reader.ReadInt32();
            fFeeRate = reader.ReadSingle();
            FeeMax = reader.ReadInt32();
            ResetType = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(Id);
            writer.Write(Level);
            writer.Write(coinType);
            writer.Write(countMax);
            writer.Write(Hour);
            writer.Write(Fee);
            writer.Write(fFeeRate);
            writer.Write(FeeMax);
            writer.Write(ResetType);
        }
    }
}
