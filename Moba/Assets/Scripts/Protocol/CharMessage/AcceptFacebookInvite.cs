// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.CharMessage
{
    /// 接受facebook邀请进入游戏
    public sealed class AcceptFacebookInvite : IProtocol
    {
        public const short MsgType = MessageType.MSG_ACCEPT_FACEBOOK_INVITE;
        public short GetMsgType { get { return MsgType; } }
        public string strInviteId;// 邀请id,多个则为一串id
        public string strFbUid;// 自己的facebook id
        public string strFbName;// 自己的facebook 名字
        
        public static void Send(string strInviteId, string strFbUid, string strFbName, object className)
        {
            var packet = new AcceptFacebookInvite
            {
                strInviteId = strInviteId,
                strFbUid = strFbUid,
                strFbName = strFbName
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            strInviteId = reader.ReadString();
            strFbUid = reader.ReadString();
            strFbName = reader.ReadString();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(strInviteId);
            writer.Write(strFbUid);
            writer.Write(strFbName);
        }
    }
}
