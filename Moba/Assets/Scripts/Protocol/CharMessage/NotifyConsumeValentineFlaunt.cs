// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.CharMessage
{
    // MSGTYPE_DECLARE(MSG_NOTIFY_CONSUME_VALENTINE_FLAUNT),
    // track->gate 请求消耗七夕鲜花
    public sealed class NotifyConsumeValentineFlaunt : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_CONSUME_VALENTINE_FLAUNT;
        public short GetMsgType { get { return MsgType; } }
        public string targetUuid;/// 目标玩家UUID
        public string targetName;/// 目标玩家名
        public uint ItemId;/// 物品ID
        public uint ItemCount;/// 物品个数
        
        public static void Send(string targetUuid, string targetName, uint itemId, uint itemCount, object className)
        {
            var packet = new NotifyConsumeValentineFlaunt
            {
                targetUuid = targetUuid,
                targetName = targetName,
                ItemId = itemId,
                ItemCount = itemCount
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            targetUuid = reader.ReadString();
            targetName = reader.ReadString();
            ItemId = reader.ReadUInt32();
            ItemCount = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(targetUuid);
            writer.Write(targetName);
            writer.Write(ItemId);
            writer.Write(ItemCount);
        }
    }
}
