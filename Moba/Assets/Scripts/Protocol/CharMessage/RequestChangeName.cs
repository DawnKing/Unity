// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.CharMessage
{
    /// 请求改名，首次改名是带性别的
    public sealed class RequestChangeName : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_CHANGE_NAME;
        public short GetMsgType { get { return MsgType; } }
        public string newName;// 新的名称
        public int sexes;// 角色性别 enum SEX_TYPE SEX_NONE表示使用物品改名
        
        public static void Send(string newName, int sexes, object className)
        {
            var packet = new RequestChangeName
            {
                newName = newName,
                sexes = sexes
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            newName = reader.ReadString();
            sexes = reader.ReadInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(newName);
            writer.Write(sexes);
        }
    }
}
