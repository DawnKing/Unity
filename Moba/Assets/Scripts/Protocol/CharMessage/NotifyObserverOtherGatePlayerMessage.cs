// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.CharMessage
{
    //通知玩家附身信息（玩家在不同gate）
    public sealed class NotifyObserverOtherGatePlayerMessage : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_OBSERVER_OTHER_GATE_PLAYER;
        public short GetMsgType { get { return MsgType; } }
        public uint observer_acct_id;//附身玩家账号ID
        public string gate_realm;//网关域名
        public uint gate_port;//网关端口
        public uint target_client_id;//附身目标ClientID
        
        public static void Send(uint observer_acct_id, string gate_realm, uint gate_port, uint target_client_id, object className)
        {
            var packet = new NotifyObserverOtherGatePlayerMessage
            {
                observer_acct_id = observer_acct_id,
                gate_realm = gate_realm,
                gate_port = gate_port,
                target_client_id = target_client_id
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            observer_acct_id = reader.ReadUInt32();
            gate_realm = reader.ReadString();
            gate_port = reader.ReadUInt32();
            target_client_id = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(observer_acct_id);
            writer.Write(gate_realm);
            writer.Write(gate_port);
            writer.Write(target_client_id);
        }
    }
}
