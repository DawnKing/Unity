// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.CharMessage
{
    /// 请求邀请facebook好友
    public sealed class RequestFacebookInvite : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_FACEBOOK_INVITE;
        public short GetMsgType { get { return MsgType; } }
        public string strInviteId;// 邀请id
        public string[] strReceiverVec;// 邀请接受者facebook id列表
        
        public static void Send(string strInviteId, string[] strReceiverVec, object className)
        {
            var packet = new RequestFacebookInvite
            {
                strInviteId = strInviteId,
                strReceiverVec = strReceiverVec
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            strInviteId = reader.ReadString();
            var length2 = reader.ReadUInt16();
            strReceiverVec = new string[length2];
            for (var i2 = 0; i2 < length2; i2++)
            {
                strReceiverVec[i2] = reader.ReadString();
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(strInviteId);
            ushort count2 = (ushort)(strReceiverVec == null ? 0 : strReceiverVec.Length);
            writer.Write(count2);
            for(var i2 = 0; i2 < count2; i2++)
            {
                writer.Write(strReceiverVec[i2]);
            }
        }
    }
}
