// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.CharMessage
{
    /// 通知改名工具的结果
    public sealed class NotifyRenameResult : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_RENAME_RET;
        public short GetMsgType { get { return MsgType; } }
        public byte result;// 改名结果：参见CharCreateFail::CVC_REASON枚举
        public string uuid;// 角色的UUID
        public string name;// 新的名称
        public int sexes;// 角色性别 enum SEX_TYPE
        
        public static void Send(byte result, string uuid, string name, int sexes, object className)
        {
            var packet = new NotifyRenameResult
            {
                result = result,
                uuid = uuid,
                name = name,
                sexes = sexes
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            result = reader.ReadByte();
            uuid = reader.ReadString();
            name = reader.ReadString();
            sexes = reader.ReadInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(result);
            writer.Write(uuid);
            writer.Write(name);
            writer.Write(sexes);
        }
    }
}
