// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.CharMessage
{
    /// MSGTYPE_DECLARE(MSG_REQUEST_SET_GUILD_SKILL)       //请求设定角色军团技能
    public sealed class RequestSetGuildSkill : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_SET_GUILD_SKILL;
        public short GetMsgType { get { return MsgType; } }
        public GuildSkillSlotInfo skillSlotInfo;// 角色军团技能槽
        
        public static void Send(GuildSkillSlotInfo skillSlotInfo, object className)
        {
            var packet = new RequestSetGuildSkill
            {
                skillSlotInfo = skillSlotInfo
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            skillSlotInfo = new GuildSkillSlotInfo();
            skillSlotInfo.ReadFromStream(reader);
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            skillSlotInfo.WriteToStream(writer);
        }
    }
}
