// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.CharMessage
{
    // MSGTYPE_DECLARE(MSG_REQUEST_CHAR_STATE),
    // 客户端查看玩家状态
    public sealed class RequestCharState : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_CHAR_STATE;
        public short GetMsgType { get { return MsgType; } }
        public string targetUuid;// 查询目标玩家Uuid
        
        public static void Send(string targetUuid, object className)
        {
            var packet = new RequestCharState
            {
                targetUuid = targetUuid
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            targetUuid = reader.ReadString();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(targetUuid);
        }
    }
}
