// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.CharMessage
{
    public sealed class NotifyPveRecord : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_PVE_RECORD;
        public short GetMsgType { get { return MsgType; } }
        public const int NORMAL_NOTIFY = 1;// 普通的通知
        public const int NEW_RECORD = 2;// 新记录
        public OnePveRecord record;// 副本记录信息
        public byte Byte;//
        
        public static void Send(OnePveRecord record, byte byte_, object className)
        {
            var packet = new NotifyPveRecord
            {
                record = record,
                Byte = byte_
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            record = new OnePveRecord();
            record.ReadFromStream(reader);
            Byte = reader.ReadByte();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            record.WriteToStream(writer);
            writer.Write(Byte);
        }
    }
}
