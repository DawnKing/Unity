// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.CharMessage
{
    /// MSGTYPE_DECLARE(MSG_NOTIFY_CASHBACK_INFO),              // 返回礼券月卡的更新信息
    public sealed class NotifyCashbackInfo : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_CASHBACK_INFO;
        public short GetMsgType { get { return MsgType; } }
        public uint cashback_times;// 领取的截止时间戳
        public uint cashback_picked;// 最后一次的领取时间（非今日则当日可领）
        
        public static void Send(uint cashback_times, uint cashback_picked, object className)
        {
            var packet = new NotifyCashbackInfo
            {
                cashback_times = cashback_times,
                cashback_picked = cashback_picked
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            cashback_times = reader.ReadUInt32();
            cashback_picked = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(cashback_times);
            writer.Write(cashback_picked);
        }
    }
}
