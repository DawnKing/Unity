// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.CharMessage
{
    // 通知ELO刷新
    public sealed class NotifyRenewElo : IProtocol
    {
        public const short MsgType = MessageType.MSG_GATE_RENEW_ELO;
        public short GetMsgType { get { return MsgType; } }
        public int type;// enum ELO_RENEW_TYPE
        public uint sessionNum;
        public uint sessionTime;
        
        public static void Send(int type, uint sessionNum, uint sessionTime, object className)
        {
            var packet = new NotifyRenewElo
            {
                type = type,
                sessionNum = sessionNum,
                sessionTime = sessionTime
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            type = reader.ReadInt32();
            sessionNum = reader.ReadUInt32();
            sessionTime = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(type);
            writer.Write(sessionNum);
            writer.Write(sessionTime);
        }
    }
}
