// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

using GameProtocol.BulkDataType;
namespace GameProtocol.CharMessage
{
    // gate->track 同步角色信息给tracksvr
    public sealed class SyncCharInfoToTrack : IProtocol
    {
        public const short MsgType = MessageType.MSG_SYNC_CHAR_INFO_TOTRACK;
        public short GetMsgType { get { return MsgType; } }
        public uint charOid;
        public byte vipLevel;// vip等级
        public int thew;// 体力值
        public uint avatar;// 角色Avatar的模板ID
        public OTHER_BRIEF otherData;// 其他简要信息
        
        public static void Send(uint charOid, byte vipLevel, int thew, uint avatar, OTHER_BRIEF otherData, object className)
        {
            var packet = new SyncCharInfoToTrack
            {
                charOid = charOid,
                vipLevel = vipLevel,
                thew = thew,
                avatar = avatar,
                otherData = otherData
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            charOid = reader.ReadUInt32();
            vipLevel = reader.ReadByte();
            thew = reader.ReadInt32();
            avatar = reader.ReadUInt32();
            otherData = new OTHER_BRIEF();
            otherData.ReadFromStream(reader);
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(charOid);
            writer.Write(vipLevel);
            writer.Write(thew);
            writer.Write(avatar);
            otherData.WriteToStream(writer);
        }
    }
}
