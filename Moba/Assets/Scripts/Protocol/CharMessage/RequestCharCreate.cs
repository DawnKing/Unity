// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.CharMessage
{
    /*
     * Client->Server：请求创建角色，服务端将返回创建选项列表
     */
    public sealed class RequestCharCreate : IProtocol
    {
        public const short MsgType = MessageType.MSG_CHARCREATE_REQUEST;
        public short GetMsgType { get { return MsgType; } }
        public int sexes;// 角色性别 enum SEX_TYPE
        public string name;// 角色名
        public string token;// 市场推广活动来源
        public string inviterUuid;// 邀请者id
        
        public static void Send(int sexes, string name, string token, string inviterUuid, object className)
        {
            var packet = new RequestCharCreate
            {
                sexes = sexes,
                name = name,
                token = token,
                inviterUuid = inviterUuid
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            sexes = reader.ReadInt32();
            name = reader.ReadString();
            token = reader.ReadString();
            inviterUuid = reader.ReadString();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(sexes);
            writer.Write(name);
            writer.Write(token);
            writer.Write(inviterUuid);
        }
    }
}
