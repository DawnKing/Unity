// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.CharMessage
{
    public sealed class NotifyHeroEnterCount : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_HERO_ENTER_COUNT;
        public short GetMsgType { get { return MsgType; } }
        public byte FreeCount;// 英雄副本剩余的免费进入次数
        public byte DayCount;// 英雄副本剩余的进入次数
        public uint BuyCount;// 英雄副本额外购买的进入次数
        public byte DayBuyLimit;// 英雄副本每日购买的上限
        
        public static void Send(byte freeCount, byte dayCount, uint buyCount, byte dayBuyLimit, object className)
        {
            var packet = new NotifyHeroEnterCount
            {
                FreeCount = freeCount,
                DayCount = dayCount,
                BuyCount = buyCount,
                DayBuyLimit = dayBuyLimit
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            FreeCount = reader.ReadByte();
            DayCount = reader.ReadByte();
            BuyCount = reader.ReadUInt32();
            DayBuyLimit = reader.ReadByte();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(FreeCount);
            writer.Write(DayCount);
            writer.Write(BuyCount);
            writer.Write(DayBuyLimit);
        }
    }
}
