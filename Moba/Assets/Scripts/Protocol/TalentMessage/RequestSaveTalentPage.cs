// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

using GameProtocol.BulkDataType;
namespace GameProtocol.TalentMessage
{
    // 请求保存天赋页
    public sealed class RequestSaveTalentPage : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_SAVE_TALENT_PAGE;
        public short GetMsgType { get { return MsgType; } }
        public TALENT_PAGE talentPage;// 天赋页信息
        
        public static void Send(TALENT_PAGE talentPage, object className)
        {
            var packet = new RequestSaveTalentPage
            {
                talentPage = talentPage
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            talentPage = new TALENT_PAGE();
            talentPage.ReadFromStream(reader);
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            talentPage.WriteToStream(writer);
        }
    }
}
