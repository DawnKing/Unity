// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TalentMessage
{
    // ----------------------------------------------------------------------
    // 天赋操作类型
    // 无
    // 添加天赋页
    // 保存天赋页
    // 重置天赋页
    // 删除天赋页
    // 设置当前天赋页
    // 请求所有天赋信息
    // 请求添加天赋页
    public sealed class RequestAddTalentPage : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_ADD_TALENT_PAGE;
        public short GetMsgType { get { return MsgType; } }
        public string strPageName;// 天赋页名字
        
        public static void Send(string strPageName, object className)
        {
            var packet = new RequestAddTalentPage
            {
                strPageName = strPageName
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            strPageName = reader.ReadString();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(strPageName);
        }
    }
}
