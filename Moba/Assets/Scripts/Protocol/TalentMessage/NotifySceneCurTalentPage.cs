// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

using GameProtocol.BulkDataType;
namespace GameProtocol.TalentMessage
{
    // 通知场景内所有玩家的当前天赋页
    public sealed class NotifySceneCurTalentPage : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_SCENE_CURTALENTPAGE;
        public short GetMsgType { get { return MsgType; } }
        public CHAR_TALENT_PAGE[] talentVec;// 玩家当前天赋信息集合
        
        public static void Send(CHAR_TALENT_PAGE[] talentVec, object className)
        {
            var packet = new NotifySceneCurTalentPage
            {
                talentVec = talentVec
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            talentVec = new CHAR_TALENT_PAGE[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                talentVec[i1] = new CHAR_TALENT_PAGE();
                talentVec[i1].ReadFromStream(reader);
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(talentVec == null ? 0 : talentVec.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                talentVec[i1].WriteToStream(writer);
            }
        }
    }
}
