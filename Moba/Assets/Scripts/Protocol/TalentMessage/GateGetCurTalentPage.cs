// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TalentMessage
{
    // global-->track-->gate 从Gate获取玩家当前天赋页
    public sealed class GateGetCurTalentPage : IProtocol
    {
        public const short MsgType = MessageType.MSG_GATE_GET_CURTALENTPAGE;
        public short GetMsgType { get { return MsgType; } }
        public byte bFromGlobal;// 是否来自Global
        
        public static void Send(byte bFromGlobal, object className)
        {
            var packet = new GateGetCurTalentPage
            {
                bFromGlobal = bFromGlobal
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            bFromGlobal = reader.ReadByte();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(bFromGlobal);
        }
    }
}
