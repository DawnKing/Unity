// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

using GameProtocol.BulkDataType;
namespace GameProtocol.TalentMessage
{
    // gate-->track-->global 向Track同步当前天赋页
    public sealed class SyncCurTalentPage : IProtocol
    {
        public const short MsgType = MessageType.MSG_SYNC_CURTALENTPAGE;
        public short GetMsgType { get { return MsgType; } }
        public byte bToGlobal;// 是否发送到Global
        public CHAR_TALENT_PAGE talentPage;// 玩家当前天赋信息
        
        public static void Send(byte bToGlobal, CHAR_TALENT_PAGE talentPage, object className)
        {
            var packet = new SyncCurTalentPage
            {
                bToGlobal = bToGlobal,
                talentPage = talentPage
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            bToGlobal = reader.ReadByte();
            talentPage = new CHAR_TALENT_PAGE();
            talentPage.ReadFromStream(reader);
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(bToGlobal);
            talentPage.WriteToStream(writer);
        }
    }
}
