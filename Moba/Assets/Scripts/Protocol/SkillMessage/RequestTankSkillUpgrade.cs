// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.SkillMessage
{
    // ----------------------------------------------------------------------
    // 技能已经满级
    // 技能还未激活
    // 角色货币不
    // 技能升级失败
    // 技能激活失败
    // 消费的货币类型错误
    // 未激活
    // 已激活，未解锁
    // 已解锁
    /// MSGTYPE_DECLARE(MSG_REQUEST_TANK_SKILL_UPGRADE)		     //客户端请求指定坦克的指定技能升级
    public sealed class RequestTankSkillUpgrade : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_TANK_SKILL_UPGRADE;
        public short GetMsgType { get { return MsgType; } }
        public string tank_uuid;
        public int skill_slot;
        public int currency_type;
        
        public static void Send(string tank_uuid, int skill_slot, int currency_type, object className)
        {
            var packet = new RequestTankSkillUpgrade
            {
                tank_uuid = tank_uuid,
                skill_slot = skill_slot,
                currency_type = currency_type
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            tank_uuid = reader.ReadString();
            skill_slot = reader.ReadInt32();
            currency_type = reader.ReadInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(tank_uuid);
            writer.Write(skill_slot);
            writer.Write(currency_type);
        }
    }
}
