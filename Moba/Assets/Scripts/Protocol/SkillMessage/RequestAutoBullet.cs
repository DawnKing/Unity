// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.SkillMessage
{
    // 请求对鼠标指针方向自动释放一个子弹
    /// MSGTYPE_DECLARE(MSG_REQUEST_AUTO_BULLET)
    public sealed class RequestAutoBullet : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_AUTO_BULLET;
        public short GetMsgType { get { return MsgType; } }
        public uint sceneId;// 战场id
        public short startX;// 炮弹发射位置
        public short startY;
        public short destX;// 攻击目标坐标
        public short destY;
        
        public static void Send(uint sceneId, short startX, short startY, short destX, short destY, object className)
        {
            var packet = new RequestAutoBullet
            {
                sceneId = sceneId,
                startX = startX,
                startY = startY,
                destX = destX,
                destY = destY
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            sceneId = reader.ReadUInt32();
            startX = reader.ReadInt16();
            startY = reader.ReadInt16();
            destX = reader.ReadInt16();
            destY = reader.ReadInt16();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(sceneId);
            writer.Write(startX);
            writer.Write(startY);
            writer.Write(destX);
            writer.Write(destY);
        }
    }
}
