// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.SystemMessage
{
    /// GM Command
    public sealed class GmGlobalGmResult : IProtocol
    {
        public const short MsgType = MessageType.MSG_GLOBAL_GM_RESULT;
        public short GetMsgType { get { return MsgType; } }
        public uint player;
        public string result;// GM命令结果
        public string gmName;// GM的角色名
        
        public static void Send(uint player, string result, string gmName, object className)
        {
            var packet = new GmGlobalGmResult
            {
                player = player,
                result = result,
                gmName = gmName
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            player = reader.ReadUInt32();
            result = reader.ReadString();
            gmName = reader.ReadString();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(player);
            writer.Write(result);
            writer.Write(gmName);
        }
    }
}
