// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.SystemMessage
{
    // MSGTYPE_DECLARE(MSG_CLIENT_UNBIND_GAME_CONNID),
    // 客户端连接绑定目标gamesvr的连接id
    public sealed class ClientUnbindGameConnId : IProtocol
    {
        public const short MsgType = MessageType.MSG_CLIENT_UNBIND_GAME_CONNID;
        public short GetMsgType { get { return MsgType; } }
        public uint gameConnId;// gamesvr连接id
        public uint AcctId;// 客户端账号id
        
        public static void Send(uint gameConnId, uint acctId, object className)
        {
            var packet = new ClientUnbindGameConnId
            {
                gameConnId = gameConnId,
                AcctId = acctId
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            gameConnId = reader.ReadUInt32();
            AcctId = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(gameConnId);
            writer.Write(AcctId);
        }
    }
}
