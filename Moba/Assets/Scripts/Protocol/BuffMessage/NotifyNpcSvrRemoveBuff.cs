// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.BuffMessage
{
    /// GameServer -> NpcServer 取消BUFF
    /// MSGTYPE_DECLARE(MSG_NOTIFY_NPC_SVR_REMOVE_BUFF),
    // 通知NPCSVR,对象失去buff
    public sealed class NotifyNpcSvrRemoveBuff : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_NPC_SVR_REMOVE_BUFF;
        public short GetMsgType { get { return MsgType; } }
        public ulong ObjId;// 对象id
        public uint BuffTpltId;//Buff模板ID
        public ulong BuffInstId;//Buff实例ID
        
        public static void Send(ulong objId, uint buffTpltId, ulong buffInstId, object className)
        {
            var packet = new NotifyNpcSvrRemoveBuff
            {
                ObjId = objId,
                BuffTpltId = buffTpltId,
                BuffInstId = buffInstId
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            ObjId = reader.ReadUInt64();
            BuffTpltId = reader.ReadUInt32();
            BuffInstId = reader.ReadUInt64();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(ObjId);
            writer.Write(BuffTpltId);
            writer.Write(BuffInstId);
        }
    }
}
