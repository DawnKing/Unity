// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

using GameProtocol.BulkDataType;
namespace GameProtocol.BuffMessage
{
    // ----------------------------------------------------------------------
    /// @file           protocol/BuffMessage.h
    /// @brief
    /// @author         Linyixiong
    /// @copyright      Sixcube Information Technology Co,. Ltd. (2012)
    /// @date           2011-5-27 16:27:00
    // -----------------------------------------------------------------------
    /// Server -> Client BUFF状态变化
    /// MSGTYPE_DECLARE(MSG_NOTIFY_ADD_BUFF),
    // 通知对象获得buff
    public sealed class NotifyAddBuff : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_ADD_BUFF;
        public short GetMsgType { get { return MsgType; } }
        public ulong ObjId;// 对象id
        public BuffInfo BuffInfo;
        public uint ExistTime;// BUFF存在时间（毫秒）
        
        public static void Send(ulong objId, BuffInfo buffInfo, uint existTime, object className)
        {
            var packet = new NotifyAddBuff
            {
                ObjId = objId,
                BuffInfo = buffInfo,
                ExistTime = existTime
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            ObjId = reader.ReadUInt64();
            BuffInfo = new BuffInfo();
            BuffInfo.ReadFromStream(reader);
            ExistTime = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(ObjId);
            BuffInfo.WriteToStream(writer);
            writer.Write(ExistTime);
        }
    }
}
