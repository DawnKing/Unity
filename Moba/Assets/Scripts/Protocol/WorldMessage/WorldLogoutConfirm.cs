// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.WorldMessage
{
    /// 角色退出游戏世界确认
    public sealed class WorldLogoutConfirm : IProtocol
    {
        public const short MsgType = MessageType.MSG_WORLD_LOGOUT_CONFIRM;
        public short GetMsgType { get { return MsgType; } }
        public uint account_id;
        public uint sector_id;
        public string serialKey;
        
        public static void Send(uint account_id, uint sector_id, string serialKey, object className)
        {
            var packet = new WorldLogoutConfirm
            {
                account_id = account_id,
                sector_id = sector_id,
                serialKey = serialKey
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            account_id = reader.ReadUInt32();
            sector_id = reader.ReadUInt32();
            serialKey = reader.ReadString();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(account_id);
            writer.Write(sector_id);
            writer.Write(serialKey);
        }
    }
}
