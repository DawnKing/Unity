// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.WorldMessage
{
    public sealed class WorldEnterRejected : IProtocol
    {
        public const short MsgType = MessageType.MSG_WORLD_ENTER_REJECTED;
        public short GetMsgType { get { return MsgType; } }
        public const int CHAR_NOTEXISTS = 0;// 角色不存在
        public const int WORLD_NOTEXISTS = 1;// 游戏世界已关闭
        public uint accountId;
        public uint reason;
        
        public static void Send(uint accountId, uint reason, object className)
        {
            var packet = new WorldEnterRejected
            {
                accountId = accountId,
                reason = reason
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            accountId = reader.ReadUInt32();
            reason = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(accountId);
            writer.Write(reason);
        }
    }
}
