// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.RoomMessage
{
    public sealed class NotifyNeedRoomPassword : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_NEED_ROOM_PASSWORD;
        public short GetMsgType { get { return MsgType; } }
        public uint RoomId;// 房间ID
        public byte RoomType;// 房间类型 enum ROOM_TYPE
        public byte Mode;// 房间模式 enum BATTLE_MODE
        
        public static void Send(uint roomId, byte roomType, byte mode, object className)
        {
            var packet = new NotifyNeedRoomPassword
            {
                RoomId = roomId,
                RoomType = roomType,
                Mode = mode
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            RoomId = reader.ReadUInt32();
            RoomType = reader.ReadByte();
            Mode = reader.ReadByte();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(RoomId);
            writer.Write(RoomType);
            writer.Write(Mode);
        }
    }
}
