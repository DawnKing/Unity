// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.RoomMessage
{
    // MSGTYPE_DECLARE(MSG_UPDATE_PLAYER_LIST),
    // 通知客户端，游戏大厅列表等级更新
    public sealed class UpdatePlayerList : IProtocol
    {
        public const short MsgType = MessageType.MSG_UPDATE_PLAYER_LIST;
        public short GetMsgType { get { return MsgType; } }
        public uint acctId;
        public int elo;// elo值
        public byte vipLevel;
        
        public static void Send(uint acctId, int elo, byte vipLevel, object className)
        {
            var packet = new UpdatePlayerList
            {
                acctId = acctId,
                elo = elo,
                vipLevel = vipLevel
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            acctId = reader.ReadUInt32();
            elo = reader.ReadInt32();
            vipLevel = reader.ReadByte();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(acctId);
            writer.Write(elo);
            writer.Write(vipLevel);
        }
    }
}
