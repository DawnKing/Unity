// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.RoomMessage
{
    // ----------------------------------------------------------------------
    ///    @file            protocol/RoomMessage.h
    ///    @brief
    ///    @author          linyixiong
    ///    @copyright       Sixcube Information Technology Co,. Ltd. (2012)
    ///    @date
    // ----------------------------------------------------------------------
    // MSGTYPE_DECLARE(MSG_REQUEST_IN_HALL),
    // 通知进入游戏大厅
    public sealed class RequestInHall : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_IN_HALL;
        public short GetMsgType { get { return MsgType; } }
        public byte Type;// 模式enum ROOM_TYPE
        public int Site;// 起始位置
        
        public static void Send(byte type, int site, object className)
        {
            var packet = new RequestInHall
            {
                Type = type,
                Site = site
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            Type = reader.ReadByte();
            Site = reader.ReadInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(Type);
            writer.Write(Site);
        }
    }
}
