// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.BattleQuestMessage
{
    // 战场任务公告类型
    // 75%完成度公告
    // 100%完成度公告
    // 通知战场任务公告
    public sealed class NotifyBattleQuestBulletin : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_BATTLE_QUEST_BULLETIN;
        public short GetMsgType { get { return MsgType; } }
        public uint nType;// 公告类型 enum BATTLE_BULLETIN_TYPE
        public uint nQusetId;// 任务id
        public uint nCamp;// 阵营
        
        public static void Send(uint nType, uint nQusetId, uint nCamp, object className)
        {
            var packet = new NotifyBattleQuestBulletin
            {
                nType = nType,
                nQusetId = nQusetId,
                nCamp = nCamp
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            nType = reader.ReadUInt32();
            nQusetId = reader.ReadUInt32();
            nCamp = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(nType);
            writer.Write(nQusetId);
            writer.Write(nCamp);
        }
    }
}
