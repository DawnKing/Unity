// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.GlobalMessage
{
    // MSGTYPE_DECLARE(MSG_GLOBAL_ASSIGN_GAMESVR),
    // 通知gate战斗分配的gamesvr
    public sealed class GlobalAssignGame : IProtocol
    {
        public const short MsgType = MessageType.MSG_GLOBAL_ASSIGN_GAMESVR;
        public short GetMsgType { get { return MsgType; } }
        public uint gameSvrId;// 服务器id
        public string gameIp;// gamesvr ip
        public string gamePort;// gamesvr port
        
        public static void Send(uint gameSvrId, string gameIp, string gamePort, object className)
        {
            var packet = new GlobalAssignGame
            {
                gameSvrId = gameSvrId,
                gameIp = gameIp,
                gamePort = gamePort
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            gameSvrId = reader.ReadUInt32();
            gameIp = reader.ReadString();
            gamePort = reader.ReadString();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(gameSvrId);
            writer.Write(gameIp);
            writer.Write(gamePort);
        }
    }
}
