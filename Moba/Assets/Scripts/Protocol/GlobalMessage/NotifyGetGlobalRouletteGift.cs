// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.GlobalMessage
{
    // MSGTYPE_DECLARE(MSG_NOTIFY_GET_GLOBAL_ROULETTE_GIFT)
    // 通知获得跨服转盘奖池的礼券
    public sealed class NotifyGetGlobalRouletteGift : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_GET_GLOBAL_ROULETTE_GIFT;
        public short GetMsgType { get { return MsgType; } }
        public uint Oid;// 角色OID
        public uint Gift;// 礼券数量
        
        public static void Send(uint oid, uint gift, object className)
        {
            var packet = new NotifyGetGlobalRouletteGift
            {
                Oid = oid,
                Gift = gift
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            Oid = reader.ReadUInt32();
            Gift = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(Oid);
            writer.Write(Gift);
        }
    }
}
