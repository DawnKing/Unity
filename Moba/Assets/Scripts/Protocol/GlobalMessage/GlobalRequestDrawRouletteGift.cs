// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.GlobalMessage
{
    // MSGTYPE_DECLARE(MSG_REQUEST_DRAW_GLOBAL_ROULETTE_GIFT)
    // 领取跨服转盘奖池的礼券
    public sealed class GlobalRequestDrawRouletteGift : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_DRAW_GLOBAL_ROULETTE_GIFT;
        public short GetMsgType { get { return MsgType; } }
        public uint Oid;// 角色OID
        public uint Rate;// 领取的奖池比例 100代表100%
        public string strName;// 角色名称
        public uint rollId;// 轮盘ID
        
        public static void Send(uint oid, uint rate, string strName, uint rollId, object className)
        {
            var packet = new GlobalRequestDrawRouletteGift
            {
                Oid = oid,
                Rate = rate,
                strName = strName,
                rollId = rollId
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            Oid = reader.ReadUInt32();
            Rate = reader.ReadUInt32();
            strName = reader.ReadString();
            rollId = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(Oid);
            writer.Write(Rate);
            writer.Write(strName);
            writer.Write(rollId);
        }
    }
}
