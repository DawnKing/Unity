// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

using GameProtocol.TeamMessage;
using GameProtocol.BulkDataType;
namespace GameProtocol.GlobalMessage
{
    // MSGTYPE_DECLARE(MSG_GLOBAL_MIDWAY_JOIN),
    // 请求中途加入
    public sealed class GlobalMidwayJoin : IProtocol
    {
        public const short MsgType = MessageType.MSG_GLOBAL_MIDWAY_JOIN;
        public short GetMsgType { get { return MsgType; } }
        public uint SceneId;// 战场id
        public CHAR_BRIEF charData;// 加入的角色
        public TeamBrief teamBrief;// 玩家战队信息
        public CHAR_TANK_BRIEF_CONT tankContVec;// 角色坦克容器列表
        public uint RoomId;// tracksvr的房间ID，用于加入战场成功后回传
        
        public static void Send(uint sceneId, CHAR_BRIEF charData, TeamBrief teamBrief, CHAR_TANK_BRIEF_CONT tankContVec, uint roomId, object className)
        {
            var packet = new GlobalMidwayJoin
            {
                SceneId = sceneId,
                charData = charData,
                teamBrief = teamBrief,
                tankContVec = tankContVec,
                RoomId = roomId
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            SceneId = reader.ReadUInt32();
            charData = new CHAR_BRIEF();
            charData.ReadFromStream(reader);
            teamBrief = new TeamBrief();
            teamBrief.ReadFromStream(reader);
            tankContVec = new CHAR_TANK_BRIEF_CONT();
            tankContVec.ReadFromStream(reader);
            RoomId = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(SceneId);
            charData.WriteToStream(writer);
            teamBrief.WriteToStream(writer);
            tankContVec.WriteToStream(writer);
            writer.Write(RoomId);
        }
    }
}
