// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

using GameProtocol.RoomMessage;
namespace GameProtocol.GlobalMessage
{
    /// global->client
    public sealed class NotifyPlayerGuildMatchSelectedTank : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_PLAYER_GUILD_MATCH_SELECTED_TANK;
        public short GetMsgType { get { return MsgType; } }
        public SelTankCount[] vecSelTank;
        
        public static void Send(SelTankCount[] vecSelTank, object className)
        {
            var packet = new NotifyPlayerGuildMatchSelectedTank
            {
                vecSelTank = vecSelTank
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            vecSelTank = new SelTankCount[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                vecSelTank[i1] = new SelTankCount();
                vecSelTank[i1].ReadFromStream(reader);
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(vecSelTank == null ? 0 : vecSelTank.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                vecSelTank[i1].WriteToStream(writer);
            }
        }
    }
}
