// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.TradeMessage
{
    /// 商品赠送给他人
    public sealed class PresentGoodsMessage : IProtocol
    {
        public const short MsgType = MessageType.MSG_GOODS_SHOP_LARGESS;
        public short GetMsgType { get { return MsgType; } }
        public uint goodsId;// 商品id
        public int price;// 赠送单价
        public int count;// 赠送数量
        public string receiverName;// 接收者名字
        public string text;// 赠言
        
        public static void Send(uint goodsId, int price, int count, string receiverName, string text, object className)
        {
            var packet = new PresentGoodsMessage
            {
                goodsId = goodsId,
                price = price,
                count = count,
                receiverName = receiverName,
                text = text
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            goodsId = reader.ReadUInt32();
            price = reader.ReadInt32();
            count = reader.ReadInt32();
            receiverName = reader.ReadString();
            text = reader.ReadString();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(goodsId);
            writer.Write(price);
            writer.Write(count);
            writer.Write(receiverName);
            writer.Write(text);
        }
    }
}
