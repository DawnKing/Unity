// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.FriendMessage
{
    /// 通知邀请好礼玩家
    public sealed class NotifyInviteGiftPlayerInviteMessage : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_INVITE_GIFT_PLAYER_INVITE;
        public short GetMsgType { get { return MsgType; } }
        public string strUuid;// 邀请者的uuid
        public string strName;// 邀请者的角色名
        
        public static void Send(string strUuid, string strName, object className)
        {
            var packet = new NotifyInviteGiftPlayerInviteMessage
            {
                strUuid = strUuid,
                strName = strName
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            strUuid = reader.ReadString();
            strName = reader.ReadString();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(strUuid);
            writer.Write(strName);
        }
    }
}
