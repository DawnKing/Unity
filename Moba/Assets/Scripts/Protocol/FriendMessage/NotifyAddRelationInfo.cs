// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

using GameProtocol.ShareTypes;
namespace GameProtocol.FriendMessage
{
    /// 通知添加关系信息
    public sealed class NotifyAddRelationInfo : IProtocol
    {
        public const short MsgType = MessageType.MSG_RELATION_ADD;
        public short GetMsgType { get { return MsgType; } }
        public int type;// 关系类型
        public BUDDY_BRIEF info;// 目标数据
        public uint killCount;// 仇敌击杀数量
        
        public static void Send(int type, BUDDY_BRIEF info, uint killCount, object className)
        {
            var packet = new NotifyAddRelationInfo
            {
                type = type,
                info = info,
                killCount = killCount
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            type = reader.ReadInt32();
            info = new BUDDY_BRIEF();
            info.ReadFromStream(reader);
            killCount = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(type);
            info.WriteToStream(writer);
            writer.Write(killCount);
        }
    }
}
