// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.ClusterMessage
{
    //MSGTYPE_DECLARE(MSG_CLUSTER_RESYNC_COMPLETE),
    //Gate->认证中心，登录同步结束
    public sealed class ClusterResyncComplete : IProtocol
    {
        public const short MsgType = MessageType.MSG_CLUSTER_RESYNC_COMPLETE;
        public short GetMsgType { get { return MsgType; } }
        public uint cur_players;//同步的玩家数量
        
        public static void Send(uint cur_players, object className)
        {
            var packet = new ClusterResyncComplete
            {
                cur_players = cur_players
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            cur_players = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(cur_players);
        }
    }
}
