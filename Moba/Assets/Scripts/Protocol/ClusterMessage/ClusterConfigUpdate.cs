// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.ClusterMessage
{
    /// MSGTYPE_DECLARE(MSG_CLUSTER_CONFIG_UPDATE)
    /// 通知群组配置信息，Adminsvr到各个服务器
    public sealed class ClusterConfigUpdate : IProtocol
    {
        public const short MsgType = MessageType.MSG_CLUSTER_CONFIG_UPDATE;
        public short GetMsgType { get { return MsgType; } }
        public ConfigEntry[] configEntries;
        
        public static void Send(ConfigEntry[] configEntries, object className)
        {
            var packet = new ClusterConfigUpdate
            {
                configEntries = configEntries
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            configEntries = new ConfigEntry[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                configEntries[i1] = new ConfigEntry();
                configEntries[i1].ReadFromStream(reader);
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(configEntries == null ? 0 : configEntries.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                configEntries[i1].WriteToStream(writer);
            }
        }
    }
}
