// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.ClusterMessage
{
    // MSGTYPE_DECLARE(MSG_NOTIFY_REFRESH_NOTE),
    // 通知刷新纪录
    public sealed class NotifyRefreshNoteMessage : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_REFRESH_NOTE;
        public short GetMsgType { get { return MsgType; } }
        public int refreshId;//刷新ID
        public uint refreshTime;//刷新时间
        
        public static void Send(int refreshId, uint refreshTime, object className)
        {
            var packet = new NotifyRefreshNoteMessage
            {
                refreshId = refreshId,
                refreshTime = refreshTime
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            refreshId = reader.ReadInt32();
            refreshTime = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(refreshId);
            writer.Write(refreshTime);
        }
    }
}
