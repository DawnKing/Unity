// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.ClusterMessage
{
    //MSGTYPE_DECLARE(MSG_CLUSTER_GAME_CLIENTLOSTCONFIRM),
    //游戏服务器->网关服务器，断线处理完毕
    public sealed class ClusterClientLostConfirm : IProtocol
    {
        public const short MsgType = MessageType.MSG_CLUSTER_GAME_CLIENTLOSTCONFIRM;
        public short GetMsgType { get { return MsgType; } }
        public uint char_oid;
        
        public static void Send(uint char_oid, object className)
        {
            var packet = new ClusterClientLostConfirm
            {
                char_oid = char_oid
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            char_oid = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(char_oid);
        }
    }
}
