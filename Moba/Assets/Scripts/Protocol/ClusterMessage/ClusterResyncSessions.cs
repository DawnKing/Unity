// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

using GameProtocol.ClusterDataType;
namespace GameProtocol.ClusterMessage
{
    //MSGTYPE_DECLARE(MSG_CLUSTER_RESYNC_SESSIONS),
    //->认证中心，同步已经完成的登陆
    public sealed class ClusterResyncSessions : IProtocol
    {
        public const short MsgType = MessageType.MSG_CLUSTER_RESYNC_SESSIONS;
        public short GetMsgType { get { return MsgType; } }
        public GAME_SESSION[] sessions;//保存记录的缓冲区
        
        public static void Send(GAME_SESSION[] sessions, object className)
        {
            var packet = new ClusterResyncSessions
            {
                sessions = sessions
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            sessions = new GAME_SESSION[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                sessions[i1] = new GAME_SESSION();
                sessions[i1].ReadFromStream(reader);
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(sessions == null ? 0 : sessions.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                sessions[i1].WriteToStream(writer);
            }
        }
    }
}
