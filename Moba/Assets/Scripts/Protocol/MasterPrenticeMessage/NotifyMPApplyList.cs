// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

using GameProtocol.ShareTypes;
namespace GameProtocol.MasterPrenticeMessage
{
    // 通知师徒申请列表
    public sealed class NotifyMPApplyList : IProtocol
    {
        public const short MsgType = MessageType.MSG_NOTIFY_MP_APPLY_LIST;
        public short GetMsgType { get { return MsgType; } }
        public BUDDY_BRIEF[] applyList;// 申请者列表
        
        public static void Send(BUDDY_BRIEF[] applyList, object className)
        {
            var packet = new NotifyMPApplyList
            {
                applyList = applyList
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            var length1 = reader.ReadUInt16();
            applyList = new BUDDY_BRIEF[length1];
            for (var i1 = 0; i1 < length1; i1++)
            {
                applyList[i1] = new BUDDY_BRIEF();
                applyList[i1].ReadFromStream(reader);
            }
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            ushort count1 = (ushort)(applyList == null ? 0 : applyList.Length);
            writer.Write(count1);
            for(var i1 = 0; i1 < count1; i1++)
            {
                applyList[i1].WriteToStream(writer);
            }
        }
    }
}
