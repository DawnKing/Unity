// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.MasterPrenticeMessage
{
    // MSGTYPE_DECLARE(MSG_REQUEST_APPLY_MP_RELATION),
    // 申请建立师徒关系
    public sealed class RequestApplyMpRelation : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_APPLY_MP_RELATION;
        public short GetMsgType { get { return MsgType; } }
        public string strUuid;// 申请目标的UUID
        public uint ApplyType;// 申请类型 MP_APPLY_TYPE
        
        public static void Send(string strUuid, uint applyType, object className)
        {
            var packet = new RequestApplyMpRelation
            {
                strUuid = strUuid,
                ApplyType = applyType
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            strUuid = reader.ReadString();
            ApplyType = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(strUuid);
            writer.Write(ApplyType);
        }
    }
}
