// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.MasterPrenticeMessage
{
    // MSGTYPE_DECLARE(MSG_UPDATE_MASTER_PRENTICE_INFO)
    // 更新师徒信息
    public sealed class NotifyUpdateMasterPrenticeInfo : IProtocol
    {
        public const short MsgType = MessageType.MSG_UPDATE_MASTER_PRENTICE_INFO;
        public short GetMsgType { get { return MsgType; } }
        public MASTER_PRENTICE_BRIEF mpBrief;// 角色师徒信息
        
        public static void Send(MASTER_PRENTICE_BRIEF mpBrief, object className)
        {
            var packet = new NotifyUpdateMasterPrenticeInfo
            {
                mpBrief = mpBrief
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            mpBrief = new MASTER_PRENTICE_BRIEF();
            mpBrief.ReadFromStream(reader);
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            mpBrief.WriteToStream(writer);
        }
    }
}
