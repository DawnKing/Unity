// Generated by proto_to_csharp.py. DO NOT EDIT!
using System.IO;

namespace GameProtocol.MasterPrenticeMessage
{
    // 请求师徒广场列表
    public sealed class RequestMPSquareList : IProtocol
    {
        public const short MsgType = MessageType.MSG_REQUEST_MP_SQUARE_LIST;
        public short GetMsgType { get { return MsgType; } }
        public const int PER_PAGE_COUNT = 10;
        public uint Type;// 请求的List类型PLAYER_LIST_TYPE
        public uint PageNum;// 页面编号
        public uint PerPageCount;// 每页的个数
        
        public static void Send(uint type, uint pageNum, uint perPageCount, object className)
        {
            var packet = new RequestMPSquareList
            {
                Type = type,
                PageNum = pageNum,
                PerPageCount = perPageCount
            };
            NetMessage.Send(packet.BuildPacket(), className);
        }
        
        public byte[] BuildPacket()
        {
            var buffer = ProtocolBuffer.Writer;
            buffer.Write(MsgType);
            buffer.Write(ProtocolBuffer.Zero);
            WriteToStream(buffer);
            return ProtocolBuffer.CacheStream.ToArray();
        }
        
        public void ReadFromStream(BinaryReader reader)
        {
            Type = reader.ReadUInt32();
            PageNum = reader.ReadUInt32();
            PerPageCount = reader.ReadUInt32();
        }
        
        public void WriteToStream(BinaryWriter writer)
        {
            writer.Write(Type);
            writer.Write(PageNum);
            writer.Write(PerPageCount);
        }
    }
}
